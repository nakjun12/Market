/**
 * NotFoundPage 컴포넌트는 사용자가 요청한 페이지가 존재하지 않을 때 보여주는 페이지입니다.
 *
 * 예시 사용법:
 * <NotFoundPage />
 *
 * @returns JSX.Element - 'notFound' 텍스트가 포함된 div 요소를 반환합니다.
 */
export default function NotFoundPage() {
  // 'notFound' 텍스트를 보여주는 div 태그를 렌더링합니다.
  return <div>notFound</div>;
}
